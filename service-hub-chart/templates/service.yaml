apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-hub-chart.fullname" . }}
  labels:
    {{- include "service-hub-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-hub-chart.selectorLabels" . | nindent 4 }}
