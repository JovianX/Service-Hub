apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-hub-chart.fullname" . }}-test-connection"
  labels:
    {{- include "service-hub-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-hub-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
